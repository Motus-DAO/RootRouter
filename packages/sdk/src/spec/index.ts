export {
  buildQueryFromSpec,
  buildSelectionFromSpec,
  extractAcceptanceCriteria,
  extractAnchorPaths,
  extractSpecTitle,
  inferPathPrefix,
  loadSpecText,
  parseSpec,
  pathMentionedInSpec,
  resolveActiveSpecPath,
} from './buildQueryFromSpec';
export type { ParsedSpec, SpecSelectionHints } from './buildQueryFromSpec';
