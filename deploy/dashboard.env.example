# Copy to deploy/dashboard.env on the VPS (not committed).
# NEXT_PUBLIC_CONVEX_URL is baked at image build time; set before docker compose build.

NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
