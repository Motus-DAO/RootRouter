# Copy to deploy/dashboard.env on VPS (optional — deploy script reads .env.local from dev machine).
# NEXT_PUBLIC_* vars are baked into the image at docker build time.

NEXT_PUBLIC_CONVEX_URL=https://notable-tortoise-269.convex.cloud
NEXT_PUBLIC_CONVEX_SITE_URL=https://notable-tortoise-269.convex.site
CONVEX_DEPLOYMENT=dev:notable-tortoise-269
